google-cloud-storage>=2.10.0
google-cloud-bigquery>=3.11.0
pandas-gbq>=0.19.2
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyspark>=3.4.0

# Note: cuDF and cuML should ideally be used in environments where they are pre-installed
# (like Google Colab T4 runtime or a Dataproc/Vertex AI container with RAPIDS).
# Installing them directly via pip can be very platform specific.
# If you are in a pure CPU environment, the pipeline is designed to gracefully
# fall back to standard pandas and scikit-learn for logic testing (though without the speedup).
